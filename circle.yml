machine:
  pre:
#    - curl -o- https://raw.githubusercontent.com/creationix/nvm/v0.26.1/install.sh | bash
#     - sudo gem install fastlane --verbose
  timezone:
    Asia/Tokyo
  xcode:
    version: "6.4"
#  node:
#    version: "0.10.34"
  environment:
    XCODE_SCHEME: armoredSkeleton
#    XCODE_PROJECT: armoredSkeleton.xcodeproj

dependencies:
  pre:
#    - pod install
#      - /bin/bash misc/keychain.sh
#    - npm install -g titanium alloy tisdk
#    - npm install

test:
  pre:
#    - gulp jshint
#     - alloy compile --config platform=ios
  override:
#        - xcodebuild test -scheme armoredSkeleton -workspace armoredSkeleton.xcworkspace 
#    - npm test

deployment:
#  adhoc:
    beta:
        branch: master
        commands:
            -   bundle exec fasten deploy
 #       - /bin/bash misc/keychain.sh
#      - sudo gem install cupertino -v 1.3.2
#      - /bin/bash misc/keychain.sh
#      - tisdk install 4.0.0.GA
#      - /bin/bash misc/adhoc.sh
#            - cd fastlane